# Leave unset for local dev - vite.config.ts proxies /api to
# http://localhost:4000 already.
#
# For any deployed build, set this to the server's origin (no trailing
# slash, no /api suffix):
#   VITE_API_URL=https://lh-transport-server.vercel.app
VITE_API_URL=
