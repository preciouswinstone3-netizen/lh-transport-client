# Used by `vite build` for the Vercel deployment only (never read by
# `npm run dev`). Points the production bundle at the live server instead
# of the local dev proxy.
VITE_API_URL=https://lh-transport-server.vercel.app
