# Used by `vite build` when deployed on Vercel. Vite inlines VITE_ vars at
# build time, so this makes the production bundle call the live API instead
# of the relative '/api' path used by the local dev proxy.
VITE_API_URL=https://lh-transport-server.vercel.app
